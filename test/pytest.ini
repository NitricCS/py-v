[pytest]
markers =
    extraction: tests related to entropy extraction modifications
    visual: tests designed to be run with pytest -s flag to observe the output
    debug: temporary mark on tests that are being debugged
    current
    regression